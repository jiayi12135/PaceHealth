# PaceHealth Flutter frontend

Run from this directory with `flutter pub get` and `flutter run`.

The app starts in mock mode so the UI is usable before Ziyan's backend is running. Set `ApiConfig.useMockData` to `false` and update `ApiConfig.baseUrl` when integrating with the backend.
