import 'package:flutter/material.dart';
class HomeScreen extends StatelessWidget { const HomeScreen({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('PaceHealth')), body: ListView(padding: const EdgeInsets.all(20), children: [Text('Good morning', style: Theme.of(context).textTheme.headlineMedium?.copyWith(fontWeight: FontWeight.bold)), const SizedBox(height: 8), const Text('Your health companion for steady progress.'), const SizedBox(height: 24), Card(color: Theme.of(context).colorScheme.primary, child: const Padding(padding: EdgeInsets.all(20), child: Text('Your personalized plan will appear here.', style: TextStyle(color: Colors.white, fontSize: 20, fontWeight: FontWeight.bold))))])); } }
