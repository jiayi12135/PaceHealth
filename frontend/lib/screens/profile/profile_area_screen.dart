import 'package:flutter/material.dart';
import '../../state/profile_store.dart';
import '../personal_information/personal_information_screen.dart';
class ProfileAreaScreen extends StatelessWidget { final ProfileStore store; const ProfileAreaScreen({super.key, required this.store}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Profile')), body: ListView(padding: const EdgeInsets.all(20), children: [CircleAvatar(radius: 38, child: Text(store.profile.name.isEmpty ? '?' : store.profile.name[0].toUpperCase(), style: const TextStyle(fontSize: 28))), const SizedBox(height: 12), Center(child: Text(store.profile.name, style: Theme.of(context).textTheme.titleLarge)), const SizedBox(height: 24), Card(child: ListTile(leading: const Icon(Icons.person), title: const Text('Personal Information'), subtitle: const Text('View your health and fitness details'), trailing: const Icon(Icons.chevron_right), onTap: () => Navigator.push(context, MaterialPageRoute(builder: (_) => PersonalInformationScreen(store: store))))), const Card(child: ListTile(leading: Icon(Icons.settings_outlined), title: Text('Settings'), subtitle: Text('Owned by ZY · integration point')))])); } }
